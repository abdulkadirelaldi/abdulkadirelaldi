## T-016 — Auth E2E: §9 senaryo 3 · `code` regresyonu · `LockoutClient` tipi

**Ajan:** Güvenlik & Test | **Öncelik:** P0 | **Bağımlılık:** T-013c, T-017
**Faz:** F1 — Veri & Auth (kapanış)
**PROGRAM.md referansı:** §9 (E2E senaryo 3), §8.4–8.6
**Kaynak:** T-013c → T1 (yeni bulgu), T2 (regresyon boşluğu)

> ### ⚠️ ÖLÇÜM GÖREVİ — tek başına koşar (ADR-023)
> Bu görev `pnpm build` / `pnpm start` / `pnpm test:e2e` kullanıyor. Dağıtıldığı sırada
> başka hiçbir ajana ölçüm görevi verilmez. Beklenmedik bir `.next` hatası görürsen
> önce paralel koşum olup olmadığını sor, sonra bulgu aç.

**Amaç:** §9'un 3. senaryosu ("yanlış şifreyle giriş başarısız, doğru şifre + 2FA ile
başarılı") artık gerçekten yazılabilir — T-013c dört hata kodunun da istemciye ulaştığını,
kurtarma kodunun çalıştığını ve kilidin yazıldığını kanıtladı. Ayrıca T-013c'nin işaret
ettiği **regresyon boşluğu** kapatılmalı.

**Regresyon boşluğu (T-013c/T2) — bu görevin asıl değeri:** `src/server/auth.ts` Vitest'in
node ortamında import edilemiyor, yani `CredentialsError extends CredentialsSignin`
davranışı **birim testiyle sabitlenemiyor**. Biri sınıfı tekrar `Error`'a çevirirse
bugün hiçbir test kırılmaz ve sorun yalnızca üretimde anlaşılır. E2E bu boşluğu kapatan
tek katman.

**Kapsam (dokunulacak):**
- `tests/e2e/auth.spec.ts` — §9 senaryo 3 + `code` regresyonu
- `src/lib/security/rate-limit.ts` — `LockoutClient` tipi (T-013c/T1)
- `docs/security/README.md` — §8 tablosu, BULGU-005 kapanışı, BULGU-006 yanlış pozitif kaydı
- `playwright.config.ts` — gerekirse ortam değişkeni ayarı

**Kapsam dışı:** `src/server/**` (Backend'in — `prismaAuthClient` adaptörü **korunur**) ·
`src/app/**`, `src/components/**` (Frontend'in, T-036 paralel) · `src/middleware.ts`
(T-014'te bitti) · CSP (F6/T-060)

**Kabul kriterleri:**
- [ ] §9/3 E2E: yanlış şifre → başarısız · doğru şifre → 2FA adımı · doğru TOTP → `/panel`
- [ ] **`code` regresyon testi:** dört kodun (`TOTP_REQUIRED`, `INVALID_CREDENTIALS`, `ACCOUNT_LOCKED`, `INVALID_TOTP`) istemciye ulaştığı E2E ile sabitlendi — `extends CredentialsSignin` kaldırılırsa **test kırılmalı**
- [ ] Kurtarma koduyla giriş E2E'de çalışıyor; kod tüketiliyor
- [ ] 5. başarısız denemede kilit → sonraki giriş `ACCOUNT_LOCKED`
- [ ] E2E, Frontend'in `AUTH_ERROR_MESSAGES` sabitini **import ediyor** — metinler elle kopyalanmıyor (T-017 sözleşmesi)
- [ ] Seçici `form [role=alert]` — yalnız `[role=alert]` Next'in `__next-route-announcer__` öğesiyle yanlış eşleşiyor (T-017 uyarısı)
- [ ] E2E test verisi izole: koşum sonrası DB seed durumuna dönüyor
- [ ] **T-013c/T1:** `LockoutClient` gerçek `PrismaClient` ile uyumlu — `const x: LockoutClient = db` derleniyor
- [ ] `docs/security/README.md`: BULGU-005 **kapalı**, BULGU-006 **yanlış pozitif** olarak kaydedilmiş (ADR-023 gerekçesiyle)
- [ ] §8 durum tablosu güncel — §8.4 ⚠️→✅ olmalı (T-013c bağladı)
- [ ] `pnpm lint && typecheck && test && test:e2e && build` temiz

**Dışarıya sözleşme:** E2E'nin gerektirdiği ortam değişkenleri ve veri durumu · `code`
regresyonunun hangi testte sabitlendiği · `LockoutClient` tipinin son hâli · §8 tablosu.
